package gr.teachspot.library.enumeration;

/** The enumeration AccountStatus contains values that indicate the {@link gr.teachspot.library.domain.Account} status. */
public enum AccountStatus {
	/** Indicates an account as active. */
	ACTIVE,
	/** Indicates an account as not active. */
	INACTIVE,
	/** Indicates an account as locked. */
	LOCKED;
}
